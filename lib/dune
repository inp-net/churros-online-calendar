(library
  (name churros_online_calendar)
  (wrapped false)
  (preprocess
      (pps ppx_deriving.show)
  )
  (modules syntax lexer parser)
)

(ocamllex lexer)

(menhir
  (modules parser)
  (flags --comment --explain)
  (infer true)
)