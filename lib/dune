(library
  (name churros_online_calendar)
  (wrapped false)
  (preprocess
      (pps ppx_deriving.show sedlex.ppx)
  )
  (modules syntax lexer parser)
  (libraries menhirLib)
)

(menhir
  (modules parser)
  (flags --comment --explain)
  (infer true)
)